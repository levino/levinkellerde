---
import Layout from './Page.astro'
import {
  MobileSidebar,
  type MobileSidebarProps,
} from '@shipyard/base/components/MobileSidebar'

const props = Astro.props
---

<Layout {...props}>
  <slot />
</Layout>
